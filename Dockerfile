# Задать родительский образ, из которого
# будет создаваться наш образ
FROM alpine:3.13

# Установить пакеты/библиотеки
RUN apk add openjdk11

# Скопировать в образ файлы и папки
COPY target/cats-api-0.0.1-SNAPSHOT.jar /app.jar

# Указать команду и аргументы для выполнения
# внутри контейнера, запущенного из образа
ENTRYPOINT ["java", "-jar", "/app.jar"]
